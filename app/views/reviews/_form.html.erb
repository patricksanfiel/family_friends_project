<%= form_with(model: @review, local: true) do |form| %>
  <% if @review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% @review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="md-form white-background-text">
    <%= form.text_area :comment, id: :review_comment, class: 'form-control text-area-description', placeholder: "Comment" %>
  </div>

  <% if @product.nil? %>
    <div class="field">
      <%= form.label :product_id %><br>
      <%= form.text_field :product_id, id: :review_product_id %>
    </div>

  <% else %>
    <%= form.hidden_field :product_id, id: :review_product_id, value: @product.id %>

  <% end %>

  <div class="actions">
    <%= form.submit class: "btn btn-md btn-primary" %>
  </div>

<% end %>
