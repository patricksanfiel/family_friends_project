<% content_for :body_class, 'seller-background' %>

<h1>All Listings</h1>
<hr>

<div class="row">

  <%= render partial: 'pages/product', collection: @products %>

</div>

<% if user_signed_in? %>

<div class="fixed-action-btn" style="bottom: 60px; right: 24px;">
  <a class="btn-floating btn-lg green" href="<%= new_product_path %>">
    <%= fa_icon "plus" %>
  </a>
</div>

<% end %>